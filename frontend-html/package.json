{
  "name": "epayco-checkout-frontend-html",
  "version": "1.0.0",
  "description": "Frontend HTML para integración con ePayco Checkout",
  "main": "index.html",
  "scripts": {
    "start": "npx serve -s . -l 3000",
    "dev": "npx serve -s . -l 3000",
    "serve": "npx serve -s . -l 3000",
    "install": "echo 'No dependencies to install for static HTML project'",
    "test": "echo 'Manual testing via browser - open http://localhost:3000' && exit 0"
  },
  "keywords": [
    "epayco",
    "checkout",
    "html",
    "frontend",
    "payments"
  ],
  "author": "ePayco Integration",
  "license": "MIT",
  "devDependencies": {
    "serve": "^14.2.1"
  }
}